using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playerState : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        battleStats player = new battleStats(10,5);
    }
}
